<div class="container-fluid py-4 px-lg-5 bg-light">
    <div
        class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-5 bg-white p-4 rounded shadow-sm border">
        <div>
            <h2 class="mb-1 text-dark">Student Dashboard</h2>
            <p class="text-muted mb-md-0">Manage Your Profile and Applications</p>
        </div>
        <div class="mt-3 mt-md-0">
            <a href="#" class="btn btn-primary shadow-sm">Find Drives</a>
        </div>
    </div>

    <div class="row g-4 mb-4">
        <!-- Student Profile Section -->
        <div class="col-xl-4 col-lg-5">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white border-bottom py-3">
                    <h5 class="mb-0 fw-bold text-dark">Student Profile</h5>
                </div>
                <div class="card-body">
                    {% set ns = namespace(found=false) %}
                    {% for student in students %}
                        {% if student.user_id == session.get('user_id') %}
                            {% set ns.found = true %}
                            <div class="mb-4 pb-3 border-bottom">
                                <h4 class="fw-bold mb-2">{{ student.first_name }} {{ student.last_name }}</h4>
                                <div class="text-muted mt-3"> 
                                    <div class="d-flex gap-2 mb-3">
                                        <a href="{{url_for('edit_student', user_id=student.user_id)}}" class="btn btn-sm btn-outline-primary">Edit Profile</a>
                                        <a href="{{url_for('delete_student', user_id=student.user_id)}}" class="btn btn-sm btn-outline-danger">Delete Profile</a>
                                    </div>
                                    <p class="mb-1"><strong>Email:</strong> {{ student.student_mail }}</p>
                                    <p class="mb-1"><strong>LinkedIn:</strong> <a href="{{ student.linkedin_profile or student.github_profile }}" target="_blank" class="text-decoration-none">{{ student.linkedin_profile or student.github_profile }}</a></p>
                                    <p class="mb-0"><strong>Contact:</strong> {{ student.contact }}</p>
                                </div>
                            </div>
                            <div>
                                <strong class="d-block mb-2 text-dark">About Student</strong>
                                <p class="text-muted mb-0" style="font-size: 0.95rem;">{{ student.address }}</p>
                            </div>
                        {% endif %}
                    {% endfor %}
                    
                    {% if not ns.found %}
                        <div class="text-center py-4">
                            <p class="text-muted mb-3">You haven't set up your profile yet.</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- #Applied Drives Section  -->
        <div class="col-xl-8 col-lg-7">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white border-bottom py-3">
                    <h5 class="mb-0 fw-bold text-dark">Drives You've Applied In</h5>
                </div>
            </div>
        </div>
    </div>
</div>